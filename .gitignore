# R
.Rhistory
.RData
.Rproj.user/
*.Rproj
.Renviron

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Raw IMF extracts are not redistributable — keep them local
data/raw/*
!data/raw/.gitkeep

# Cached estimation output is regenerated by the scripts.
# Uncomment to keep the repository light; by default the cache ships so that
# figures and tables rebuild in seconds without a multi-hour run.
# output/tables/raw_*.csv

# Large scratch files
*.tmp
*.log
!output/logs/*.log
